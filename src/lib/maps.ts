// Shared Google Maps JS API loader options. Both map components MUST use the
// same options object — @react-google-maps/api throws if the loader is called
// twice with different options during client-side navigation.
export const GOOGLE_MAPS_LIBRARIES: "places"[] = ["places"];
export const GOOGLE_MAPS_LOADER_ID = "fibernorth-gmaps";
